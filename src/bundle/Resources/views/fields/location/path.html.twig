{% spaceless %}
    {% for location in locations %}
        <a href="{{ path('_ezpublishLocation', {'locationId': location.id}) }}">{{ ez_content_name(location.contentInfo) }}</a>
        {% if not loop.last %}
        /
        {% endif %}
    {% endfor %}
{% endspaceless %}